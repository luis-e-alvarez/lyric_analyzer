{/* <div className='col-md-4'>
            <h3>Values</h3>
            {props.values.map((item,i) => {
               return [<h4>{item.name},{item.percentile}</h4>]
            })}
          </div>
          <div className='col-md-4'>
            <h3>Consumption Preferences</h3>
           {props.consumptionPreference.map((item,i) => {
              return [<h4>{item.name}, {item.score}</h4>]
           })}
         </div>


values={this.state.values} consumptionPreference={this.state.consumptionPreference} */}